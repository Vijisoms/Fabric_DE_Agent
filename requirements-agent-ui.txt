# Dependencies for Agent Framework + DevUI (local web UI)
# Keeps the MCP server container small by not adding these to requirements.txt
#
# NOTE: At the time of writing, these may be published as pre-release packages.
# Install with: python -m pip install --pre -r requirements-agent-ui.txt

agent-framework
agent-framework-azure-ai
agent-framework-devui

azure-identity
python-dotenv
