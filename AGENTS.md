# AGENTS

General guidance for AI agents working in this repo:
- Prefer modifying code under `src/`.
- Keep MCP tool signatures stable (tool names + params).
- Do not commit secrets.
- Put VS Code Copilot customization files under `.github/`.
